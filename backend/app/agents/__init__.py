"""MAF agent package: read-only analysis tools, a deterministic intent router (works with no
LLM key), the Azure chat-client factory, specialist agents, orchestrator, and the answer
service that ties in the semantic cache."""
