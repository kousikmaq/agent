"""Cross-cutting infrastructure concerns (logging, exceptions, error handlers).

The :mod:`app.core` package holds framework-level plumbing that is shared by
every feature module but contains no business logic.
"""
